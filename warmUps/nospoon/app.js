/*var mug = {
    color: "black",
    price: 25,
    isThermos: true,
    write: function() { 
    }
}
console.log("My favorite mug is " + mug.color + " that cost $ " + mug.price + " is it a thermos? " + mug.isThermos);

var phone = {
    price: 500,
    color: "space gray",
    model: "8 plus",
    write: function() {
    }
}
console.log("fancy " + phone.model + " costs $ " + phone.price + " in color " + phone.color);

var buddy = {
    name: "Phil",
    age: 30,
    homeTown: "Kansas",
    write: function() {
    }
}
console.log(buddy.name + " is " + buddy.age + " years old, " + "from " + buddy.homeTown);

var glasses = {
    style: "lennon",
    lenses: "mirrored",
    price: 1,
    write: function() {
    }
}
console.log("My " + glasses.style + " glasses cost me $" + glasses.price + " the lenses are not tinted but are " + glasses.lenses);

var laptop = {
    model: "Macbook Pro",
    color: "gray and black",
    price: "too much damn money lol",
    write: function() {
    }
}
console.log("Vschool likes us to use a " + laptop.model + " that comes in a cool " + laptop.color + " color, that cost... " + laptop.price);

var laptopCase = {
    color: "gray",
    brand: "Tomtoo",
    from: "Vschool",
    write: function() {
    }
}
console.log(laptopCase.from + " provides it's students with a " + laptopCase.color + " " + laptopCase.brand ,"case")*/

var spoon = {
    color: "silver",
    type: "wooden",
    value: "probably not",
    price: 10000000
}
console.log("Auctioning: fancy $" + spoon.price + " dollar " + spoon.color +" "+ spoon.type + " spoon... should this be used?: " + spoon.value)